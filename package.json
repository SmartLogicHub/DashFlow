{
  "name": "dashflow",
  "version": "0.1.9",
  "description": "A modular multi-dashboard workspace for tasks, projects, habits, calendar planning and weekly review inside Obsidian.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "test": "node tests/run-layout-tests.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "keywords": ["obsidian", "dashboard", "multi-dashboard", "tasks", "projects", "habits", "calendar", "agenda", "activity", "heatmap", "weekly-review", "mobile"],
  "devDependencies": {
    "@types/node": "^22.15.17",
    "esbuild": "0.25.5",
    "obsidian": "latest",
    "typescript": "^5.8.3"
  }
}
