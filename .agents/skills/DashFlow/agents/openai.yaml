interface:
  display_name: "DashFlow"
  short_description: "Repo-specific patterns and workflows for DashFlow"
  default_prompt: "Use the DashFlow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true