# DashFlow 0.6.1 Audit Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Release a regression-tested DashFlow 0.6.1 patch that safely loads malformed schema-8 settings, recovers from dismissed onboarding, and ships a complete installable package.

**Architecture:** Preserve the existing migration and recovery contract. Extend validation at the single data-loading boundary, make modal completion state explicit, and package the existing generated assets without changing runtime asset resolution.

**Tech Stack:** TypeScript 5.8, Obsidian API 1.11.4, Node 22 test runner, GitHub Actions, Bash/zip.

---

## File Map

- Modify `src/core/pluginDataMigration.ts`: normalize missing setting keys and reject incompatible value types before casting schema-8 data.
- Modify `src/ui/OnboardingModal.ts` and `src/main.ts`: separate modal dismissal from successful onboarding completion.
- Modify `tests/plugin-data-migration.test.ts`, `tests/onboarding.test.ts`, and `tests/release-metadata.test.ts`: add regressions before code.
- Modify release metadata, documentation, `.gitignore`, and GitHub workflows for patch version and complete archives.

### Task 1: Harden schema-8 settings

- [ ] Add a test whose otherwise-valid schema-8 data includes a numeric `homeHeroImagePath`; expect recovery without persistence.
- [ ] Run the focused suite and observe the expected failure: invalid settings load as normal data.
- [ ] Add a primitive settings validator/normalizer at the migration boundary and use it in the schema-8 branch.
- [ ] Re-run the focused suite and then all tests.

### Task 2: Release onboarding dismissal lock

- [ ] Add a regression test requiring a distinct dismissal callback and lock release path.
- [ ] Run the focused suite and observe the expected missing lifecycle wiring.
- [ ] Track completed onboarding before closing the modal; call the dismissal callback only for non-completion closes; clear `onboardingOpen` in the plugin dismissal callback.
- [ ] Re-run the focused suite and then all tests.

### Task 3: Package and version the patch

- [ ] Add metadata and workflow tests requiring 0.6.1, a ZIP archive with `assets/heroes/`, and complete source inputs.
- [ ] Run the focused suite and observe the expected release-metadata failure.
- [ ] Bump all release metadata to 0.6.1, update lock metadata, create the release ZIP in the workflow, add install instructions, include source inputs, and ignore local `main.js`.
- [ ] Run all checks and inspect the ZIP file listing.

### Task 4: Integration

- [ ] Commit the focused patch on `fix/dashflow-0.6.1-audit`.
- [ ] Merge the verified commit into `main`, push it, and confirm CI plus the v0.6.1 GitHub release.
